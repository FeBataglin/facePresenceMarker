export interface ISessionInfo {
    diStatus: string;
    productVersion: string;
    userPwd: string;
    sessionId: string;
    userDomain: string;
    userName: string;
    userSO: string;
    timeout: number;
}
export declare class SessionInfo implements ISessionInfo {
    diStatus: string;
    productVersion: string;
    userPwd: string;
    sessionId: string;
    userDomain: string;
    userName: string;
    userSO: string;
    timeout: number;
    constructor(values?: object);
    get $diStatus(): string;
    get $productVersion(): string;
    get $userPwd(): string;
    get $sessionId(): string;
    get $userDomain(): string;
    get $userName(): string;
    get $userSO(): string;
    get $timeout(): number;
    set $diStatus(diStatus: string);
    set $productVersion(productVersion: string);
    set $userPwd(userPwd: string);
    set $sessionId(sessionId: string);
    set $userDomain(userDomain: string);
    set $userName(userName: string);
    set $userSO(userSO: string);
    set $timeout(timeout: number);
    static of(json?: any): SessionInfo;
    static empty(): SessionInfo;
    static fromJson(json?: Array<any>): ISessionInfo[];
}
